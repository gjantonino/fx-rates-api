const fixerio = require('./fixerio-api/index.js');
const config  = require('./config.js');
const healthcheck = require('./healthcheck.js');

module.exports = {
    fixerio,
    config,
    healthcheck
}